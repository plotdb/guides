# Docker 環境說明


## Port Mapping

環境狀況：
 - 此環境可能運行於 Docker 容器中
 - 用戶啟動容器時可能有設定 port mapping (格式：`-p 外部:內部`)
 - 容器內無法查詢 port mapping 設定

LLM 行為準則：
 1. 啟動任何 server/服務前，必須先詢問用戶 port mapping 設定
 2. 使用「內部 port」啟動服務
 3. 服務需監聽 `0.0.0.0` (而非 `127.0.0.1`) 才能從外部訪問
